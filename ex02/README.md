# Tạo thư mục làm việc trong Ubuntu

```
WORKDIR /app
```

`WORKDIR` create folder command
`/app` create folder app in the root folder -> move to app folder after creating it
-> build again -> run image
